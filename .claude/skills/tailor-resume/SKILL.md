---
name: tailor-resume
description: Use when the user pastes/links a job description and wants their resume tailored to it for submission — e.g. "tailor my resume to this JD", "align my resume with this job posting", "make a resume for this role". Reads the canonical resume data from About.js (never edits it), figures out what the job actually wants, and produces a tailored About.js-shaped data file plus a matching cover-letter data file, both registered in the site's /admin resume list, so the user can download PDFs (generated by the site's own generators) for that specific application at any time. Do not use for editing the live site's About page content — that's a normal code-edit request, not this skill.
---

# Tailor Resume to a Job Description

## Source of truth

[About.js](../../../About.js) at the repo root is the **canonical** source of factual content: `about`,
`skills`, `education`, `experience`, `timeline` (per-role bullet points in `Description`, split by `●`),
`Projects`, `certificates`. Dates, titles, employers, and degrees always come from here — never invent one
that isn't already in this file.

`resume/knowledge/<company-slug>.json` files (written by the [capture-experience](../capture-experience/SKILL.md)
skill) are an optional **supplementary** source of pre-vetted bullets for a specific employer — richer or
alternately-phrased material than what made it onto the public site. If a JD's target company has a matching
knowledge file, you may draw bullets from its `roles[].bullets` / `projects[].bullets` (each already carries a
`source` back to something the user actually said, so this doesn't violate the no-fabrication rule) — but the
role's `title`/dates/`companyname` still come from `About.js`, not the knowledge file. If no knowledge file
exists for the company, that's fine — just work from `About.js` as before.

**Never edit About.js.** It drives the live site. This skill only ever writes new files under `resume/tailored/`.

## How the output gets downloaded

The site has no build step and no backend — `index.html` loads `About.js` as a plain global and its "Download
Resume" button calls `generateResumePdf(about, filename)` from [js/resume-pdf.js](../../../js/resume-pdf.js)
(pdfMake, client-side) to produce the PDF. `/admin/resumes.html` is a password-gated page (client-side MD5
check only — see Security note below) that lists every tailored resume from `resume/tailored/manifest.json`
and, on click, fetches that entry's JSON data file and calls the exact same `generateResumePdf()` function.

The cover letter works the same way: `js/cover-letter-pdf.js` exposes `generateCoverLetterPdf(letter, filename)`,
which `/admin/resumes.html` calls when the user clicks "Download Cover Letter" on an entry that has a
`coverLetterFile`. It renders `applicant` (`name`/`email`/`phone`/`linkedIn`/`website`), `date`, `recipient`
(`name`/`company`/`roleTitle`), `salutation`, `paragraphs` (array of strings, one per rendered paragraph),
`closing`, and `signatureName`.

This means: **this skill never renders a PDF itself.** It only produces the tailored data files + a manifest
entry; the PDFs are generated fresh, in-browser, whenever the user clicks Download in `/admin`. It also means
the output format is constrained to what the generators actually render. `generateResumePdf()` renders `name`,
`email`, `phone`, `linkedIn`, `gitHub`, `website`, `skills`, `timeline` (with `title`/`companyname`/`location`/
`RO`/`status`/`StartDate`/`EndDate`/`Description`), `education`, `Projects`. **It does not render
`certificates`** — don't spend effort selecting a certificates subset, it has nowhere to go in this pipeline.
`generateCoverLetterPdf()` renders only the fields listed above — no skills/timeline/projects section, it's a
letter, not a resume.

## Inputs

The user provides a job description as pasted text, a file path, or a URL (fetch it if it's a URL). If they
only give a company/role name with no JD text, ask them to paste the posting before proceeding — tailoring
without the actual text is just guessing.

## Process

1. **Read `About.js`** fully to refresh on the current data (it changes over time as the user updates their
   site — don't rely on memory from a previous run). Also check whether `resume/knowledge/` contains a file
   for the JD's company (slugify the company name the same way step 6 does) — if so, read it too.

2. **Extract signal from the JD**: role title, seniority, must-have hard skills/stack, nice-to-haves, domain
   (e.g. insurance, fintech, supply chain), and recurring phrasing an ATS or recruiter would scan for.

3. **Cross-reference against About.js** (and the matching knowledge file, if one exists) — for each JD
   requirement, find the matching evidence already present (a skill in `skills`, a bullet in some
   `timeline[year].work[n].Description`, a `Projects` entry, or a tagged bullet in a `resume/knowledge/*.json`
   role/project). If a JD requirement has **no** matching evidence anywhere in either source, do not paper
   over it — note it to the user as a gap rather than inventing a bullet.

4. **Draft tailored content**, built only from real About.js (and knowledge-file) material:
   - **Skills**: the real skills list from `skills`, reordered so JD-matching items lead each category. Don't
     drop skills, just resequence — this stays truthful and still reads as comprehensive.
   - **Experience**: for each role, pull the `●`-delimited bullets from `Description` and select/reorder the
     ones most relevant to this JD (light rephrasing for keyword alignment is fine; changing facts or numbers
     is not). If a knowledge file exists for that company, you may also pull in bullets from its matching
     `roles[].bullets` whose `tags` line up with the JD — use the knowledge file's bullet `text` verbatim or
     lightly rephrased, never its `source` field (that's internal provenance, not resume copy). Include the
     roles that best support the JD — usually all of them for a senior-level posting, but trim clearly
     irrelevant older roles if space/relevance calls for it.
   - **Projects**: include 0–2 entries from `Projects` (or a knowledge file's `projects[]`, same rule as
     above) only if directly relevant to the JD.
   - **Education**: include as-is.

5. **Build the tailored data file** — a plain JSON object shaped like the subset of About.js described above
   (same field names/nesting, including the `{year: {work: {n: {...}}}}` timeline shape so the generator's
   sort-by-year-descending still works). Only include the roles/bullets/projects selected in step 4 — this
   file represents the trimmed, reordered resume, not the full About.js.

6. **Draft the cover letter**, built only from the same real material used for the resume (About.js text,
   `about` bio, and any matching knowledge-file bullets) — no claims here that aren't already backed by
   something in the resume you just built:
   - 3–4 short paragraphs: (1) which role/company and a one-line hook connecting the candidate's background to
     what the JD is asking for, (2) 1–2 concrete pieces of evidence from `timeline`/knowledge-file bullets that
     map to the JD's top requirements (not a re-listing of the whole resume — pick the 2–3 strongest matches),
     (3) optional: address the biggest gap noted in step 3 briefly and positively if it's worth acknowledging,
     otherwise skip this paragraph, (4) a short close expressing interest and availability.
   - Tone: professional, direct, first person, no filler ("I am writing to express my interest..." clichés are
     fine sparingly but don't lean on them for every paragraph).
   - `recipient.name` defaults to `"Hiring Manager"` unless the JD names an actual person. `recipient.company`
     comes from the JD. `recipient.roleTitle` is the JD's role title.
   - `date` is today's date in long form (e.g. `"July 17, 2026"`).
   - `applicant` block mirrors `About.js`: `name`, `email`, `phone`, `linkedIn`, `website`.
   - Build the JSON shaped as:
     ```json
     {
       "applicant": { "name": "...", "email": "...", "phone": "...", "linkedIn": "...", "website": "..." },
       "date": "<Month DD, YYYY>",
       "recipient": { "name": "Hiring Manager", "company": "<Company>", "roleTitle": "<Role>" },
       "salutation": "Dear Hiring Manager,",
       "paragraphs": ["...", "...", "..."],
       "closing": "Sincerely,",
       "signatureName": "<Applicant Name>"
     }
     ```

7. **Slugify** company + role (lowercase, hyphens) for an id, e.g. `acme-corp-senior-backend-engineer`. Write
   the resume JSON to `resume/tailored/<slug>.json` and the cover letter JSON to
   `resume/tailored/<slug>-cover-letter.json`. Create `resume/tailored/` if it doesn't exist.

8. **Update the manifest** at `resume/tailored/manifest.json` (a JSON array — read it, don't overwrite other
   entries). Append (or replace, if re-running for the same slug):
   ```json
   {
     "id": "<slug>",
     "title": "<Role>",
     "description": "<one sentence on what was emphasized>",
     "date": "<YYYY-MM-DDTHH:mm>",
     "dataFile": "resume/tailored/<slug>.json",
     "coverLetterFile": "resume/tailored/<slug>-cover-letter.json"
   }
   ```
   `title` is the **role/position only** (e.g. ".NET Developer"), not the target company — the whole point is
   that the same tailored resume is reusable for any other company hiring the same position, so the company
   name shouldn't be baked into the label the user sees in `/admin`. If the original target company is worth
   noting for the user's own tracking, work it into `description` instead (e.g. "Tailored from the Acme Corp
   posting; emphasized..."). Use the current local date and time in `YYYY-MM-DDTHH:mm` form (not just the
   date) — `/admin` displays and sorts entries newest-first by this field, so it must be accurate. Keep the
   description to one sentence — it's shown as subtext in the admin list, not a changelog. `coverLetterFile`
   is what makes `/admin` show the "Download Cover Letter" button — omit it only if the user explicitly says
   they don't want a cover letter for this run.

9. **Report**: the slug/id, a reminder that both PDFs are downloaded from `/admin/resumes.html` (not this
   session), a short list of what was emphasized/reordered/trimmed in the resume and why, the angle taken in
   the cover letter, and any JD requirements that had no supporting evidence in About.js. Offer to adjust on
   feedback.

## Security note (tell the user if it's their first time using this)

The public repo backs the live site, so anything under `resume/tailored/` is a public static file the moment
it's pushed — reachable directly by URL even though `/admin` has a password screen. The admin password (MD5
hash in `admin/auth.json`) is a client-side-only check: it stops a casual visitor from finding the resume
list, but it is not real access control. Don't tailor a resume through this pipeline for anything more
sensitive than what's already public in About.js (which is the whole site's premise anyway).

## Guardrails

- No fabricated employers, titles, dates, degrees, metrics, or skills — ever. Reordering, selecting, and
  light rephrasing of existing content is fine; adding new claims is not.
- Don't modify `About.js`.
- Don't modify `resume/knowledge/*.json` — those are owned by the `capture-experience` skill; this skill only
  reads them.
- Don't modify `js/resume-pdf.js`, `js/cover-letter-pdf.js`, or `admin/*` as part of a normal tailoring run —
  those are site infrastructure, not per-application output.
- Cover letter paragraphs must stay traceable to the resume/knowledge material, same as resume bullets — no
  invented projects, metrics, or claims that don't already appear in About.js or the matching knowledge file.
- `resume/tailored/` is **not** gitignored and must be committed + pushed for `/admin` to see it on the live
  site — this is a deliberate difference from the old `resume/output/` behavior. Tell the user a commit/push
  is needed if they want the new entry visible on the deployed site.
- If the user wants the *site's* About page content changed, that's a separate, normal code-edit task — don't
  route it through this skill.
